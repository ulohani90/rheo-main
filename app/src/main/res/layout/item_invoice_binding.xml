<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.rheotv.android.ui.activities.tabcontainer.InvoiceItemViewModel" />
    </data>

    <androidx.cardview.widget.CardView
        android:id="@+id/leaderBoardCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:layout_marginStart="@dimen/margin_10"
        android:layout_marginTop="@dimen/margin_8"
        android:layout_marginEnd="@dimen/margin_10"
        android:onClick="@{() -> viewModel.onCardClicked()}"
        app:cardBackgroundColor="#202020"
        app:cardCornerRadius="@dimen/margin_6"
        app:cardElevation="@dimen/margin_8">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="32dp"
                android:layout_height="28dp"
                android:layout_gravity="center_vertical"
                android:layout_marginStart="@dimen/margin_30"
                android:background="@drawable/money_credits" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="1"
                android:paddingStart="@dimen/margin_16"
                android:paddingTop="@dimen/margin_12"
                android:paddingEnd="@dimen/margin_16"
                android:paddingBottom="@dimen/margin_12"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="start"
                    android:text="@{viewModel.title}"
                    android:textColor="@color/white"
                    android:textSize="@dimen/text_20"
                    tools:text="MARCH" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="start"
                    android:paddingTop="@dimen/margin_4"
                    android:text="@{viewModel.description}"
                    android:textColor="@color/white"
                    android:textSize="@dimen/text_14"
                    tools:text="Money Credited - 25$" />

            </LinearLayout>
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>