<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="code"
            type="String" />

    </data>

    <TextView
        android:id="@+id/voucher_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_4"
        android:background="@drawable/dashed_rectangle"
        android:drawableEnd="@drawable/avd_copy"
        android:drawableTint="@color/color_selected"
        android:padding="@dimen/margin_16"
        android:text="@{code}"
        android:textColor="@color/color_selected"
        tools:text="test1IDR12k-00495" />

</layout>