<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#32425c"
    android:padding="@dimen/margin_4">

    <ImageView
        android:id="@+id/poster_image_view"
        android:layout_width="58dp"
        android:layout_height="58dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_margin="@dimen/margin_8"
        tools:src="@tools:sample/avatars" />

    <TextView
        android:id="@+id/title_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/poster_image_view"
        android:layout_marginEnd="@dimen/margin_16"
        android:layout_toEndOf="@id/poster_image_view"
        android:fontFamily="sans-serif"
        android:text="Rheo"
        android:textColor="@color/light_grey_text_color"
        android:textSize="13sp"
        android:textStyle="normal" />

    <TextView
        android:id="@+id/sub_header_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/title_text_view"
        android:layout_alignBaseline="@id/close_button"
        android:layout_alignStart="@id/title_text_view"
        android:layout_toStartOf="@+id/close_button"
        android:ellipsize="end"
        android:fontFamily="sans-serif-medium"
        android:maxLines="1"
        android:textColor="#fbfbfb"
        android:textSize="15sp"
        android:textStyle="normal"
        tools:text="Rheo Tournament live" />

    <TextView
        android:id="@+id/detail_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/sub_header_text_view"
        android:layout_alignStart="@id/sub_header_text_view"
        android:fontFamily="sans-serif"
        android:textColor="@color/light_grey_text_color"
        android:textSize="13sp"
        android:textStyle="normal"
        tools:text="Rahul, since 2h 40m" />

    <ImageView
        android:id="@+id/close_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="@dimen/margin_4"
        android:layout_marginEnd="@dimen/margin_8"
        android:src="@drawable/avd_back_white" />

    <ImageView
        android:id="@+id/volume_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/poster_image_view"
        android:layout_alignStart="@id/detail_text_view"
        android:layout_marginTop="@dimen/margin_8"
        android:layout_marginBottom="@dimen/margin_8"
        tools:src="@drawable/avd_unmute" />

    <ImageView
        android:id="@+id/play_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/volume_button"
        android:layout_alignBottom="@id/volume_button"
        android:layout_marginStart="@dimen/margin_32"
        android:layout_toEndOf="@id/volume_button"
        tools:src="@drawable/ic_pause_button_white_24dp" />

    <ImageView
        android:id="@+id/heart_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/play_button"
        android:layout_alignBottom="@id/play_button"
        android:layout_marginStart="@dimen/margin_32"
        android:layout_toEndOf="@id/play_button"
        tools:src="@drawable/avd_heart_outline" />

    <TextView
        android:id="@+id/view_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/heart_button"
        android:layout_alignParentEnd="true"
        android:drawableEnd="@drawable/ic_keyboard_arrow_right_white_24dp"
        android:drawablePadding="0dp"
        android:drawableTint="@color/light_grey_text_color"
        android:gravity="center"
        android:text="@string/view"
        android:textColor="@color/white" />

</RelativeLayout>